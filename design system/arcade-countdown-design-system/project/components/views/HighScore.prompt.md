The day-eight completion state — an arcade high-score board listing every day that landed.

```jsx
<HighScore rows={[{label:'Notice of intent', value:'03 Sep'}, …]}
  footnote="Cabinet stays open. Come back whenever." />
```

- The ONE screen where pink leads instead of accenting. Everywhere else pink is a single value.
- No confetti, no burst, no sound. The restraint is the celebration.
